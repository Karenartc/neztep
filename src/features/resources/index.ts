// Public API of the resources feature — types only.
// Import hooks from    @/features/resources/hooks/use-resources      (client components only)
// Import services from @/features/resources/services/resources-client (client components only)
export type { Resource, ResourceCategory } from "./types";
