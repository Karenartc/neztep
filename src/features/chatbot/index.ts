// Public API of the chatbot feature — types only.
// Import hooks from    @/features/chatbot/hooks/use-chatbot      (client components only)
// Import services from @/features/chatbot/services/chatbot-client (client components only)
export type { ChatMessage, ChatSession, MessageRole, SendMessagePayload } from "./types";
