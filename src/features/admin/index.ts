// Public API of the admin feature — types only.
// Import hooks from    @/features/admin/hooks/use-admin      (client components only)
// Import services from @/features/admin/services/admin-client (client components only)
export type {
  ContentItem,
  ContentStatus,
  CreateContentPayload,
  InstitutionMetrics,
} from "./types";
